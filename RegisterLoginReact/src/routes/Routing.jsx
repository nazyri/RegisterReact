import { BrowserRouter as Router, Routes, Route } from 'react-router-dom';
const Routing = () => {
  return (
    <Router>
     <Routes>
       <Route/>
     </Routes>
    </Router>
   );
};
export default Routing;